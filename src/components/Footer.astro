---
const legalLinks = [
    'Terms',
    'Privacy',
    'Cookies'
];

const menuItemsProduct = [
    'Features',
    'Documentation',
    'APM',
    'Support',
    'Integrations',
    'Resources',
    'Alerts',
    'Security',
    'API',
    'Pricing'
];

const menuItemsAbout = [
    'Contact Us',
    'Partners',
    'Press',
    'Our Team',
    'Careers',
    'Legal'
];

const menuItemsSocial = [
    'Blog',
    'Espanol',
    'Japanese',
    'Instagram',
    'Twitter'
];
---

<footer class="px-8 py-10 bg-gradient-to-b from-datadogGray to-datadogGrayDark text-datadogStone">
    <div class="md:ml-40 md:mr-10">
        <section class="flex justify-between">
            <div class="flex flex-col justify-between">
                <a class="free-trial">Free Trial</a>
                <p class="copywrite">&copy; Datadog 2017</p>
            </div>
            <menu class="flex gap-8 w-2/3">
                <!-- TODO: make compoent for menu groups below; Need to solve for the uniqueness of Product group (grid classes applied to <ul> achieve desired layout) -->
                <li class="grow">
                    <strong>Product</strong>
                    <!-- TODO: is there another layout approach that uses a 2-column layout in which content overflows from the left col to the right col? -->
                    <ul class="grid grid-cols-2 border-t-2 border-datadogStone mt-2 pt-2">
                        {menuItemsProduct.map((item) => <li><a class="hover:underline" href="#">{item}</a></li>)}
                    </ul>
                </li>
                <li class="grow">
                    <strong>About</strong>
                    <ul class="border-t-2 border-datadogStone mt-2 pt-2">
                        {menuItemsAbout.map((item) => <li><a class="hover:underline" href="#">{item}</a></li>)}
                    </ul>
                </li>
                <li class="grow">
                    <strong>Social</strong>
                    <ul class="border-t-2 border-datadogStone mt-2 pt-2">
                        {menuItemsSocial.map((item) => <li><a class="hover:underline" href="#">{item}</a></li>)}
                    </ul>
                </li>
            </menu>
        </section>

        <section class="flex justify-center divide-x-2 mt-20 text-center text-white">
            {legalLinks.map((link) => <a class="inline-block px-3 hover:underline" href="#">{link}</a>)}
        </section>
    </div>
</footer>